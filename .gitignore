# HAR test fixtures -- contain real capture data, not for public commit
build/tests/fixtures/

# Local handoff artifacts from previous session
.gitignored/
_handoff/
HANDOFF.md

# Node.js / build tooling
node_modules/
*.pyc
__pycache__/

# Temp working files
.pr-body.md

# Generated build artifacts (regenerated by build/build.py)
extension/engine.js

# Packaged extension zips (regenerated by build/package.py)
dist/

# Baseline snapshot zip: kept on disk as a local backup, not in history
harstack-baseline.zip
